#! /bin/bash

# build & launch
sudo docker-compose up --build

# test
#CONTAINER=simplemsaapps_app1_1

#sudo docker exec -it ${CONTAINER} curl localhost:5000

#sudo docker exec -it ${CONTAINER} curl localhost:5000?sleep=0

#sudo docker exec -it ${CONTAINER} curl localhost:5000?sleep=2

#sudo docker exec -it ${CONTAINER} curl localhost:5000?sleep=7


